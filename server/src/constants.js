const ErrorCodes = {
  AuthenticationFailure: 'authentication_failure',
}

const MessageTypes = {
  Text: 'text',
  Sticker: 'sticker',
  Image: 'image',
  Video: 'video',
}

module.exports = {
  ErrorCodes,
  MessageTypes,
}
